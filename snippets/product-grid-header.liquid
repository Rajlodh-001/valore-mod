{% liquid
  if results.url
    assign results_url = results.url
  else
    assign terms = results.terms | escape
    assign results_url = '?q=' | append: terms | append: '&options%5Bprefix%5D=last&sort_by=' | append: sort_by
  endif

  assign sort_by = results.sort_by | default: results.default_sort_by
%}

<div class="active-facets">
  {%- for filter in results.filters -%}
    {%- for value in filter.active_values -%}
      <facet-remove>
        <a
          href="{{ value.url_to_remove }}"
          class="active-facets__button btn btn--small"
          data-tippy-content="{{ 'products.facets.clear_with_label' | t: label: value.label | escape }}"
          data-tippy-theme="color-background-1"
          data-tippy-placement="top"
        >
          <span class="active-facets__button-inner">
            {{ value.label | escape }}
            {% comment %} theme-check-disable {% endcomment %}
            {{ 'icon-close-small.svg' | inline_asset_content }}
            {% comment %} theme-check-enable {% endcomment %}
          </span>
        </a>
      </facet-remove>
    {%- endfor -%}
    {% if filter.type == 'price_range' %}
      {%- if filter.min_value.value != null or filter.max_value.value != null -%}
        <facet-remove>
          <a href="{{ filter.url_to_remove }}" class="active-facets__button btn btn--small">
            <span class="active-facets__button-inner">
              {%- if filter.min_value.value -%}
                {{ filter.min_value.value | money }}
              {%- else -%}
                {{ 0 | money }}
              {%- endif -%}
              -
              {%- if filter.max_value.value -%}
                {{ filter.max_value.value | money }}
              {%- else -%}
                {{ filter.range_max | money }}
              {%- endif -%}
              {% comment %} theme-check-disable {% endcomment %}
              {{ 'icon-close-small.svg' | inline_asset_content }}
              {% comment %} theme-check-enable {% endcomment %}
              <span class="visually-hidden">{{ 'products.facets.clear_filter' | t }}</span>
            </span>
          </a>
        </facet-remove>
      {%- endif -%}
    {% endif %}
  {%- endfor -%}
  <facet-remove>
    <a href="{{ results_url }}" class="active-facets__button btn btn--small btn--underline active-facets__button-clear">
      <span>{{ 'products.facets.clear_all' | t }}</span>
    </a>
  </facet-remove>
</div>

{% if enable_product_count or enable_sorting or enable_filtering %}
  {% if prestige_style %}
    <div class="prestige-collection-toolbar" data-prestige-collection-toolbar>
      <div class="prestige-collection-toolbar__layouts no-js-hidden">
        <button type="button" class="prestige-collection-toolbar__btn prestige-collection-toolbar__layout-btn hidden md:inline-flex" data-prestige-desktop-columns="4" aria-label="Compact grid">
          <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><rect x="1" y="1" width="3" height="3"/><rect x="6" y="1" width="3" height="3"/><rect x="11" y="1" width="3" height="3"/><rect x="1" y="6" width="3" height="3"/><rect x="6" y="6" width="3" height="3"/><rect x="11" y="6" width="3" height="3"/></svg>
        </button>
        <button type="button" class="prestige-collection-toolbar__btn prestige-collection-toolbar__layout-btn hidden md:inline-flex" data-prestige-desktop-columns="3" aria-label="Medium grid">
          <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><rect x="1" y="1" width="4" height="4"/><rect x="6" y="1" width="4" height="4"/><rect x="11" y="1" width="4" height="4"/><rect x="1" y="7" width="4" height="4"/><rect x="6" y="7" width="4" height="4"/><rect x="11" y="7" width="4" height="4"/></svg>
        </button>
        <button type="button" class="prestige-collection-toolbar__btn prestige-collection-toolbar__layout-btn hidden md:inline-flex" data-prestige-desktop-columns="2" aria-label="Large grid">
          <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><rect x="1" y="1" width="6" height="6"/><rect x="9" y="1" width="6" height="6"/><rect x="1" y="9" width="6" height="6"/><rect x="9" y="9" width="6" height="6"/></svg>
        </button>
        <div class="column-switcher flex no-js-hidden items-center md:hidden">
          <button class="prestige-collection-toolbar__btn prestige-collection-toolbar__layout-btn" data-column="1" aria-label="1 column">
            <svg width="12" height="12" fill="currentColor" viewBox="0 0 12.5 9.5"><path d="M12.5.75a.76.76 0 01-.75.75h-11A.76.76 0 010 .75.76.76 0 01.75 0h11a.76.76 0 01.75.75zM12.5 4.75a.76.76 0 01-.75.75h-11A.76.76 0 010 4.75.76.76 0 01.75 4h11a.76.76 0 01.75.75zM12.5 8.75a.76.76 0 01-.75.75h-11A.76.76 0 010 8.75.76.76 0 01.75 8h11a.76.76 0 01.75.75z"/></svg>
          </button>
          <button class="prestige-collection-toolbar__btn prestige-collection-toolbar__layout-btn is-active" aria-selected="true" data-column="2" aria-label="2 columns">
            <svg width="12" height="12" fill="currentColor" viewBox="0 0 5.5 12.5"><path d="M.75 0a.76.76 0 01.75.75v11a.76.76 0 01-.75.75.76.76 0 01-.75-.75v-11A.76.76 0 01.75 0zM4.75 0a.76.76 0 01.75.75v11a.76.76 0 01-.75.75.76.76 0 01-.75-.75v-11A.76.76 0 014.75 0z"/></svg>
          </button>
        </div>
      </div>

      {% if enable_product_count %}
        <p class="prestige-collection-toolbar__count" role="status" data-product-count>
          {{ results.products_count }} PRODUCTS
        </p>
      {% endif %}

      <div class="prestige-collection-toolbar__actions">
        {% if enable_sorting %}
          <div class="prestige-sort no-js-hidden">
            <button type="button" class="prestige-collection-toolbar__btn" data-prestige-sort-toggle aria-expanded="false" aria-controls="PrestigeSortPanel">
              {{ 'products.facets.sort_by_label' | t | upcase }}
              <svg width="10" height="6" viewBox="0 0 10 6" fill="currentColor" aria-hidden="true"><path d="M1 1l4 4 4-4"/></svg>
            </button>
            <div id="PrestigeSortPanel" class="prestige-sort__panel" data-prestige-sort-panel hidden>
              {%- for option in results.sort_options -%}
                <button
                  type="button"
                  class="prestige-sort__option{% if option.value == sort_by %} is-selected{% endif %}"
                  data-prestige-sort-option="{{ option.value }}"
                >
                  {{ option.name }}
                </button>
              {%- endfor -%}
            </div>
            <div class="visually-hidden">
              <facet-filters-form class="f-facets">
                <form id="FacetSortForm">
                  <select id="SortBy" name="sort_by" aria-hidden="true" tabindex="-1">
                    {%- for option in results.sort_options -%}
                      <option value="{{ option.value }}" {% if option.value == sort_by %}selected="selected"{% endif %}>{{ option.name }}</option>
                    {%- endfor -%}
                  </select>
                </form>
              </facet-filters-form>
            </div>
          </div>
        {% endif %}

        {% if enable_filtering %}
          <drawer-opener data-drawer="#Drawer-Facets" class="no-js-hidden">
            <button type="button" class="prestige-collection-toolbar__btn" aria-label="{{ 'products.facets.filter_button' | t }}">
              {{ 'products.facets.filter_button' | t | upcase }}
            </button>
          </drawer-opener>
        {% endif %}
      </div>
    </div>
  {% else %}
    <div class="product-grid__header flex items-center justify-between">
      {% if enable_filtering %}
        <drawer-opener
          data-drawer="#Drawer-Facets"
          class="no-js-hidden {% unless sidebar_position == 'drawer' %}hidden lg-down:block{% endunless %}"
        >
          <button
            class="btn--filter-sort btn btn--outline btn--secondary btn--with-icon font-body"
            aria-label="{{- 'products.facets.filter_button' | t -}}"
          >
            {% comment %} theme-check-disable {% endcomment %}
            {{ 'icon-filter.svg' | inline_asset_content }}
            {% comment %} theme-check-enable {% endcomment %}
            <span class="text-normal">{{- 'products.facets.filter_and_sort' | t -}}</span>
          </button>
        </drawer-opener>
        <noscript>
          <details class="drawer-facets-nojs parent-group {% unless sidebar_position == 'drawer' %}hidden lg-down:block{% endunless %}">
            <summary>
              <span class="btn btn--plain btn--with-icon">
                {% comment %} theme-check-disable {% endcomment %}
                {{ 'icon-filter.svg' | inline_asset_content }}
                {% comment %} theme-check-enable {% endcomment %}
                <span>{{- 'products.facets.filter_button' | t -}}</span></span
              >
              {% render 'facets-nojs', results: results, form_id: 'facets-2', sidebar_position: sidebar_position %}
            </summary>
          </details>
        </noscript>
      {% endif %}
      {% if enable_product_count %}
        <div class="collection-product-count hidden md:block" role="status" data-product-count>
          {%- if results.results_count -%}
            {{- 'templates.search.results_with_count' | t: terms: results.terms, count: results.results_count -}}
          {%- elsif results.products_count == results.all_products_count -%}
            {{- 'products.facets.product_count_simple' | t: count: results.products_count -}}
          {%- else -%}
            {{-
              'products.facets.product_count'
              | t: product_count: results.products_count, count: results.all_products_count
            -}}
          {%- endif -%}
        </div>
      {% endif %}
      {% if enable_sorting %}
        <div class="collection-sortby hidden md:block no-js-hidden">
          <label for="SortBy" class="visually-hidden">{{ 'products.facets.sort_by_label' | t }}:</label>
          <facet-filters-form class="f-facets">
            <form id="FacetSortForm">
              <select
                id="SortBy"
                class="form-control"
                name="sort_by"
                aria-describedby="a11y-refresh-page-message a11y-selection-message"
                data-default-sortby="{{ collection.default_sort_by }}"
              >
                {%- for option in results.sort_options -%}
                  <option
                    value="{{ option.value }}"
                    {% if option.value == sort_by %}
                      selected="selected"
                    {% endif %}
                  >
                    {{ option.name }}
                  </option>
                {%- endfor -%}
              </select>
            </form>
          </facet-filters-form>
        </div>
      {% endif %}

      <div class="column-switcher flex no-js-hidden items-center justify-end md:hidden">
        <button class="btn btn--icon" data-column="1" aria-label="1 column">
          <svg width="12" height="12" fill="currentColor" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 12.5 9.5">
            <path d="M12.5.75a.76.76 0 01-.75.75h-11A.76.76 0 010 .75.76.76 0 01.75.75zM12.5 4.75a.76.76 0 01-.75.75h-11A.76.76 0 010 4.75.76.76 0 01.75 4h11a.76.76 0 01.75.75zM12.5 8.75a.76.76 0 01-.75.75h-11A.76.76 0 010 8.75.76.76 0 01.75 8h11a.76.76 0 01.75.75z"/>
          </svg>
        </button>
        <button class="btn btn--icon" aria-selected="true" data-column="2" aria-label="2 columns">
          <svg width="12" height="12" fill="currentColor" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 5.5 12.5">
            <path d="M.75 0a.76.76 0 01.75.75v11a.76.76 0 01-.75.75.76.76 0 01-.75-.75v-11A.76.76 0 01.75 0zM4.75 0a.76.76 0 01.75.75v11a.76.76 0 01-.75.75.76.76 0 01-.75-.75v-11A.76.76 0 014.75 0z"/>
          </svg>
        </button>
      </div>
    </div>
  {% endif %}
{% endif %}
